/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["www.gstatic.com", "a.slack-edge.com", "cdn.iconscout.com", "wac-cdn.atlassian.com", "www.notion.so", "aem.dropbox.com"]
    }
};

export default nextConfig;
